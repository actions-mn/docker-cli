name: test

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    name: basic metanorma tests
    steps:
    - uses: actions/checkout@v2

    - uses: ./
      with:
        args: [ help ]

    - uses: ./
      with:
        args: [ version ]